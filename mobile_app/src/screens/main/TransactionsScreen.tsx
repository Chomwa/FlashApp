/**
 * TransactionsScreen - Full transaction history view
 * Uses the same logic as ActivityScreen for consistency
 */
import ActivityScreen from './ActivityScreen';

// Export ActivityScreen as TransactionsScreen for consistency
// Both screens show the same transaction history
export default ActivityScreen;